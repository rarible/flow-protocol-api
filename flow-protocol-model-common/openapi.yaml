openapi: 3.0.3
info:
servers:
paths:

components:
  schemas:

    Word:
      type: string

    Binary:
      type: string

    BigInteger:
      type: string
      example: 717802

    BigDecimal:
      type: string
      example: 717802.342336

    Address:
      type: object
      properties:
        value:
          type: string
